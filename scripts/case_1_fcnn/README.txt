Running the Test Case

To execute the full test pipeline, run:

./run.sh

This script will:

- Process and prepare the dataset
- Train the neural network
- Test and evaluate the trained model

Ensure all required dependencies listed in requirements.txt are installed before running the script.
